Each of the three scripts in tools/ (csvsum.py, wordcount.py, slugify.py) has one bug, and each has a test in tests/ (test_<name>.py). Fix them in parallel: in one reply, call agent_start three times with wait false, one child per script, each told to fix its script and run its test. Do not fix anything yourself. When the three answers have arrived, run all three tests once and report.
