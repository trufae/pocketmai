Each of the three scripts in tools/ (csvsum.py, wordcount.py, slugify.py) has one bug, and each has a test in tests/ (test_<name>.py). In one reply, call agent_start three times with wait false, one child per script, each told to fix its script and run its test. Then answer with the single word "waiting" and nothing else; do not call agent_result or agent_status, the children's answers arrive on their own as messages. Once all three answers have arrived, run the three tests once and report.
